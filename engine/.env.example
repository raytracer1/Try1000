# Backend URL
TRY1000_BACKEND_URL=http://localhost:8000

# Ably key for real-time task dispatch (optional — poll mode if unset)
TRY1000_ABLY_KEY=

# Poll mode (set to 1 if no Ably key)
TRY1000_POLL=1

# Poll interval in seconds
TRY1000_POLL_INTERVAL=5
